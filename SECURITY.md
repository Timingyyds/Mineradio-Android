# 安全说明

## 支持范围

仅维护最新公开版本。

## 密钥与签名

⚠️ **本仓库包含完整的签名密钥与运行时密钥**，这是作者的明确决定，
以便任何人都能构建出与正式版完全一致的 APK。

具体包含哪些、以及公开签名密钥意味着什么，请务必阅读
[SECURITY_NOTICE.md](./SECURITY_NOTICE.md)。

**如果你要发布自己的版本，请务必：**

1. 替换 `key.jks` 为你自己的签名密钥 —— 不要复用本仓库的
2. 替换资源加密口令，并保证构建脚本、Kotlin、Node、C++ 四处一致
3. 用你自己的 RSA 密钥重新生成 `mr_private_key.h` 分片

公开可见的签名密钥**无法防止**他人伪造同包名、同签名的安装包。

## 构建安全建议

1. `key.jks` / `keystore.properties` 必须留在本地，已在 `.gitignore` 中排除
2. 资源加密口令请自行替换为私有值，并保证构建脚本、Kotlin、Node、C++ 四处一致
3. 提交前确认没有把登录 Cookie 或 token 文件带进版本库

## 报告安全问题

如发现安全问题，请通过 GitHub Issues 或仓库作者主页联系。

**请不要在公开 Issue 中直接贴出** Cookie、Token、账号信息、私密链接或可复现的敏感数据。
如果必须提供日志，请先删除其中的 Cookie、Token 与本机路径。

## 敏感数据清单

以下内容不应出现在 Issue、日志或提交中：

- 登录 Cookie / Token
- 账号截图或二维码登录状态
- 本机文件路径
- 签名密钥与口令
- 调试日志中的完整请求头
